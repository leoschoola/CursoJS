function soma (n1=0,n2=0) { //n1=0 é apenas para, se nao mencionado o parametro, não aparecer NaN, e o unico parametro mencionado ser somado com 0
    return n1 + n2
}

console.log(soma(2,5)) //soma recebe 2 e 5 como parametro